# Cloudflare Pages Deploy

- Root directory: `.`
- Build command: `(none)`
- Output directory: `site`

Notes:

- This deploy is the public docs/landing surface for the CLI.
- The CLI itself remains the primary product surface.
